fn main() -> i64 {
  var zone = zone::create(64);
  var vec = std::vec::Vec<i64> {
    raw: std::vec::RawVec<i64> {
      data: 0u64 as ptr i64,
      len: 0,
      capacity: 0,
    },
  };

  vec.reserve(ref mut zone, 1);

  zone::destroy(zone);
  return 0;
}
