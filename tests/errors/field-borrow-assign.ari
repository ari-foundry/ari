struct Pair {
  mut left: i64,
  mut right: i64,
}

fn inspect(value: ref i64) -> i64 {
  return 0;
}

fn main() -> i64 {
  var pair = Pair { left: 1, right: 2 };
  let left_ref = ref pair.left;
  pair.left = 3;
  return inspect(left_ref);
}
