fn stop_after_call(flag: bool) -> i64 {
  if flag {
    return 16;
  }
  std::panic();
}

fn main() -> i64 {
  return Some(10).unwrap() +
         Ok<i64, i32>(12).unwrap() +
         (Err<i64, i32>(14i32).unwrap_err() as i64) +
         stop_after_call(true);
}
