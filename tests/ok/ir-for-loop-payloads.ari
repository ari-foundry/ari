fn main() -> i64 {
  var total = 0;

  for value in range(0, 4) {
    total = total + value;
  }

  for inclusive in range_inclusive(1, 3) {
    total = total + inclusive;
  }

  for item in [4, 5] {
    total = total + item;
  }

  return total;
}
