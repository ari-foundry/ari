# Cross-Library Smoke Standard Library Tests

This folder contains positive tests for the `smoke` standard-library feature group. Keep each `.ari` file focused on one API family or lowering behavior, and update `tests/Makefile` whenever the fixture should be part of automated checks.
