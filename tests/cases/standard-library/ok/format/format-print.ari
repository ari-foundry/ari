fn main() -> i64 {
  println("value={} ok={} byte={}", -3, true, 65 as u8);
  print("{{literal}} {}\n", 12);
  io::println("qualified={}", 2);
  println("text={} quoted={:?}", "ari", "ari");
  println("debug={:?}", false);
  return 0;
}
