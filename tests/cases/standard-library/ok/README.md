# Standard Library Positive Tests

Valid source `std` programs are grouped by feature below this folder. Each feature folder contains Ari programs that should compile, and many are also linked and executed by `make check-prelude`.

Add new tests to the folder matching the module or prelude surface being exercised.
