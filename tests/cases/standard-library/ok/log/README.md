# Logging Standard Library Tests

This folder contains positive tests for `std::log`, the source-only stderr
logging helper module.

- `std-log-basic.ari`: level ordering, enable checks, byte-slice logging,
  string-message logging, convenience level functions, and stderr output shape.
