# Test Support Standard Library Tests

This folder contains positive tests for `std::test`, the small source helper
module for executable unit-style tests.

- `std-test-report.ari`: report aggregation, generic equality helpers,
  method wrappers, explicit finish status, and scratch zone creation.
