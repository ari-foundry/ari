# Linked List Tests

This folder covers `std::collections::LinkedList[T]`, the zone-backed
index-node doubly linked list.

- `std-collections-linked-list.ari`: checks front/back pushes, node-slot reuse
  after removal, indexed lookup/removal, optional helpers, iteration, and the
  root `LinkedList` alias.
