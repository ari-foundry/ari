# Heap Tests

This folder covers `std::collections::BinaryHeap[T]` and
`std::collections::PriorityQueue[T]`.

- `std-collections-heap.ari`: checks max-priority ordering with
  `collections::less_i64`, growth, peek/pop/try-pop behavior, and the root
  `BinaryHeap` and `PriorityQueue` aliases.
