# Standard Library Parse Tests

These cases cover source-only text parsers in `std::parse`.

Current files:

- `std-parse-basic.ari`: ASCII-trimmed signed integer, bool, and decimal
  float parsing, including invalid whole-input cases.
