# Standard Library Algorithm Tests

These cases cover source-only algorithms in `std::algo`.

Current files:

- `std-algo-slice-helpers.ari`: sorting, stable sorting, binary search,
  reverse/rotate, partition, min/max/clamp, swap, fill, copy, and dedup over
  borrowed `Slice` views.
