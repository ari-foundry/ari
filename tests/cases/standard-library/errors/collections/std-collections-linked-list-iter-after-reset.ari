fn main() -> i64 {
  var zone = zone::create(256);
  var list = LinkedList::new<i64>(ref mut zone, 1);
  list.push_back(ref mut zone, 1);
  var cursor = list.iter();
  zone::reset(ref mut zone);
  let value = cursor.next();

  zone::destroy(zone);
  return 0;
}
