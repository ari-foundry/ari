# Standard Library Diagnostic Tests

Invalid source `std` programs are grouped by feature below this folder. Each file should lock down one diagnostic or one closely related diagnostic family.

Add new files to the folder matching the API family that rejects the program.
