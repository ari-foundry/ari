CompilerStagePlan source=tests/cases/compiler-development/artifact/ok/token-dump-basic.ari target=x86_64-pc-linux-gnu implicit_std=false module_search_paths=0 cfg_features=0
  1 StagePlan owner=driver artifact=--emit-stage-plan first_check="make check-compiler-artifacts" proves="artifact ladder, layer owners, and first checks"
  2 SourceMap owner=driver artifact=--emit-source-map first_check="make check-compiler-artifacts" proves="source files, byte offsets, line starts, and snippets"
  3 Tokens owner=lexer artifact=--emit-tokens first_check="make check-compiler-artifacts" proves="token kinds, spellings, and byte spans"
  4 Diagnostics owner=diagnostics artifact=--emit-diagnostics first_check="make check-compiler-artifacts" proves="stable error code families and normalized messages"
  5 DiagnosticCatalog owner=diagnostics artifact=--emit-diagnostic-catalog first_check="make check-compiler-artifacts" proves="diagnostic codes, layer families, and owning source files"
  6 Syntax owner=parser artifact=--emit-syntax first_check="make check-compiler-artifacts" proves="AST shape and parser recovery"
  7 ModuleGraph owner=module-loader artifact=--emit-module-graph first_check="make check-compiler-artifacts" proves="file-backed modules, imports, visibility, and item surfaces"
  8 DeclarationIndex owner=declaration-collector artifact=--emit-declaration-index first_check="make check-compiler-artifacts" proves="declaration signatures, visibility, and source locations"
  9 FutureHIR owner=lowering/resolver artifact=planned --emit-hir first_check="future check-compiler-artifacts" proves="lowered syntax and resolver-facing node shapes"
  10 TypedIR owner=sema artifact=--emit-typed-ir first_check="make check-compiler-artifacts" proves="type, trait, ownership, and lowering facts"
  11 PassSummary owner=driver/sema artifact=--emit-pass-summary first_check="make check-compiler-artifacts" proves="stage counts and pass boundaries"
  12 LLVM owner=llvm-backend artifact=--emit-llvm first_check="focused --emit-llvm" proves="backend lowering after earlier artifacts match"
  13 Object owner=toolchain artifact=--emit-obj first_check="focused --emit-obj" proves="object emission and exported symbol surface"
  14 Executable owner=toolchain artifact=-o first_check="focused linked run" proves="final runtime behavior"
  DevelopmentGate source_identity=required diagnostics=required modules=required artifacts=ordered executable=last
