CompilerCapabilityInventory version=1 target=x86_64-pc-linux-gnu implicit_std=false entries=28
  capability=functions status=implemented owner=parser/sema/backend first_check="make check-functions" proves="function declarations, calls, returns, and main entry points"
  capability=locals-and-assignment status=implemented owner=parser/sema first_check="make check-variables" proves="let, var, assignment, and local value flow"
  capability=control-flow status=implemented owner=parser/sema/backend first_check="make check-control-flow" proves="if, while, break, continue, init-while-next, and range loops"
  capability=integer-and-bool-scalars status=implemented owner=lexer/sema/backend first_check="make check-operators" proves="signed and unsigned integers, bool values, casts, comparisons, and bit operations"
  capability=ownership-checks status=implemented owner=ownership/control-flow first_check="focused ownership fixtures" proves="own, drop, move state, and temporary borrow rejection"
  capability=non-generic-enums-and-match status=implemented owner=parser/sema/backend first_check="make check-match" proves="one-word tagged enums and statement match lowering"
  capability=inline-modules-and-use status=implemented owner=module-loader/sema first_check="make check-modules" proves="inline modules, pub visibility, use imports, and qualified paths"
  capability=file-backed-modules status=implemented owner=module-loader first_check="make check-modules" proves="module source loading, graph source ids, metadata, cache validation, cycles, ambiguity checks, and project fixtures"
  capability=prelude-io-formatting status=implemented owner=prelude/sema/backend first_check="focused format fixtures" proves="compiler-known print, println, and formatting hooks"
  capability=extern-c-ffi status=implemented owner=parser/sema/backend first_check="make check-ffi" proves="extern C declarations, explicit link names, and C symbol calls"
  capability=shared-library-output status=implemented owner=driver/backend/toolchain first_check="focused --shared fixture" proves="LLVM shared library emission and exported Ari symbols"
  capability=generic-function-calls status=implemented owner=sema/backend first_check="make check-generics" proves="declarations, explicit/inferred calls, deterministic specializations, module-qualified calls, function pointer specialization, trait bounds, and diagnostics"
  capability=structs-and-field-layout status=implemented owner=parser/sema/backend first_check="make check-structs" proves="named structs, tuple structs, field access/assignment, aggregate layout/codegen, match destructuring, and diagnostics"
  capability=trait-resolution status=implemented owner=type/trait-semantics first_check="make check-traits" proves="minimum static trait subset, impl conformance, deterministic dispatch, bounds, and trait diagnostics"
  capability=typed-ir-and-llvm-path status=implemented owner=sema/llvm-backend first_check="make check-compiler-artifacts" proves="typed IR, LLVM text, object output, and executable emission on the LLVM path"
  capability=diagnostic-artifacts status=partial owner=diagnostics/driver first_check="make check-compiler-artifacts" proves="stable diagnostic codes, catalog, source fields, and transitional message classification"
  capability=source-identity-artifacts status=partial owner=driver/source-loader first_check="make check-compiler-artifacts" proves="source maps, byte offsets, line tables, snippets, and normalized artifact paths"
  capability=compiler-capability-inventory status=implemented owner=driver first_check="make check-compiler-artifacts" proves="machine-readable public compiler surface for contributor triage"
  capability=resolver-facing-artifact status=implemented owner=declaration-collector first_check="make check-compiler-artifacts" proves="module imports, uses, declarations, signatures, visibility, and stable source locations before typed IR"
  capability=generic-aggregate-monomorphization status=implemented owner=type/sema/backend first_check="make check-generics" proves="generic structs, enums, aliases, nested payload layout, ownership, and compiler-shaped fixtures"
  capability=structural-capability-parameters status=partial owner=parser/type/trait-diagnostics first_check="targeted parser diagnostic fixture" proves="parameter-local single and grouped has-method capability syntax for free functions and inherent impl methods, static call-site method checks, and trait-quality diagnostics without an interface keyword"
  capability=union-by-fields status=partial owner=parser/type/ownership/backend first_check="make check-structs" proves="discriminant-linked union fields with stable selector validation, enum-selector construction via field: arm => payload, same-literal selector checks, and direct field match reading"
  capability=function-parameter-patterns status=planned owner=parser/sema first_check="future parser and sema fixtures" proves="patterns in function parameter positions"
  capability=runtime-strings-and-floats status=planned owner=runtime/backend first_check="future std and backend fixtures" proves="owned runtime strings and complete float value semantics"
  capability=raw-pointers-and-allocation-zones status=planned owner=type/ownership first_check="future ownership diagnostics" proves="explicit pointer operations, allocation-zone rules, and diagnostics"
  capability=general-iterator-protocol status=planned owner=traits/sema/backend first_check="future iterator fixtures" proves="iterator dispatch beyond compiler-known range loops"
  capability=class-keyword status=rejected owner=parser/policy first_check="make check-errors" proves="Ari uses structs, enums, functions, and traits instead of class syntax"
  capability=interface-keyword status=rejected owner=parser/policy first_check="make check-errors" proves="Ari uses trait for abstraction boundaries instead of interface syntax"
