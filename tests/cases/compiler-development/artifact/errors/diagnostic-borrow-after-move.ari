fn consume(value: own i64) -> i64 {
  drop value;
  return 0;
}

fn inspect(value: ref i64) -> i64 {
  return 0;
}

fn main() -> i64 {
  let token: own i64 = 7;
  consume(token);
  return inspect(ref token);
}
