// Covers compiler-development policy: Ari uses `trait`, not a bootstrap-only
// `interface` keyword for compiler abstraction boundaries.
interface CompilerPass {
  fn run() -> i64
}

fn main() -> i64 {
  return 0;
}
