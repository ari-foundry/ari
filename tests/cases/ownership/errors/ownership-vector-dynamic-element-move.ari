fn make_owned(value: i64) -> own i64 {
  return value;
}

fn main() -> i64 {
  let values: Vec[own i64] = [make_owned(1), make_owned(2)];
  let index = 0;
  let first = values[index];
  drop first;
  drop values;
  return 0;
}
