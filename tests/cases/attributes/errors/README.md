# Attribute And Meta Attribute Errors Tests

This folder contains expected diagnostics for Ari attribute and meta attribute behavior. File names should describe the exact feature or diagnostic being locked down.

Add new fixtures to the relevant `tests/Makefile` target when they should run in CI.
