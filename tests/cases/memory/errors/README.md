# Memory And Zones Errors Tests

This folder contains expected diagnostics for Ari memory and zones behavior. File names should describe the exact feature or diagnostic being locked down.

Add new fixtures to the relevant `tests/Makefile` target when they should run in CI.
