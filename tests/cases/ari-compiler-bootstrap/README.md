# Ari Compiler Bootstrap Tests

These fixtures exercise the actual Ari-written compiler source root in
`compiler/` through the current stage0 compiler.

Keep this directory for bootstrap source-root smoke tests. Compiler-shaped
hosted-compiler readiness fixtures still live under
`tests/cases/bootstrap-readiness/`.
