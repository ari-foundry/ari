# Structs Ok Tests

This folder contains valid programs for Ari structs behavior. File names should describe the exact feature or diagnostic being locked down.

Add new fixtures to the relevant `tests/Makefile` target when they should run in CI.
